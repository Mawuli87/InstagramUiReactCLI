import {StyleSheet} from 'react-native';
import {horizontalScale} from '../../style/scaling';

const style = StyleSheet.create({
  userImageContainer: {
    borderWidth: 1,
    borderColor: '#F35BAC',
    padding: horizontalScale(2.5),
    borderRadius: horizontalScale(50),
  },
});
export default style;
